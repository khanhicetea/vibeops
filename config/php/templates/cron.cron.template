# Generated by manage.py
# App: ${APP_NAME}
# Job: ${JOB_NAME}
# PHP: ${PHP_VERSION} (${PHP_SERVICE})
CRON_TZ=${TIMEZONE}
${SCHEDULE} /usr/local/bin/php-cron-job ${QUOTED_JOB_NAME} ${QUOTED_WORKDIR} ${OUTPUT} ${TIMEOUT} ${QUOTED_LOCK} ${PHP_SERVICE} ${QUOTED_COMMAND}
