# Generated by VibeOps manage.py — do not commit this file.
# Host/service name is reachable from PHP containers on the backend network.

MYSQL_HOST=${MYSQL_SERVICE}
MYSQL_PORT=3306
MYSQL_DATABASE_PREFIX=${USERNAME}_
MYSQL_USER=${USERNAME}
MYSQL_PASSWORD=${MYSQL_PASSWORD}

# Laravel / many PHP apps
DB_HOST=${MYSQL_SERVICE}
DB_PORT=3306
DB_USERNAME=${USERNAME}
DB_PASSWORD=${MYSQL_PASSWORD}
# DB_DATABASE is per-app: {prefix}{db_suffix} e.g. ${USERNAME}_app
