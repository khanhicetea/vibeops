[mysqld]
# MySQL 8.4 LTS defaults for modern apps.
disable_log_bin
mysql_native_password = ON
sql_require_primary_key = OFF

# Prefer the 8.x redo log capacity setting.
innodb_buffer_pool_size = 512M
innodb_redo_log_capacity = 512M
