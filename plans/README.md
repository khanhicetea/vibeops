# Implementation Plans

Generated by the improve skill on 2026-07-09 (MySQL perf / stability / DX focus). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Planned against:** `7ed5180` (`7ed5180ca236f79dcaeec8257a0882f6946bc558`)

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Tame MySQL per-session buffer memory | P1 | S | — | DONE |
| 002 | Add MySQL healthcheck and healthy depends_on | P1 | S | — | DONE |
| 003 | Stop passing MySQL root password on process argv | P1 | S | — | DONE |
| 004 | Env-driven InnoDB buffer pool sizing | P1 | S | 001 | DONE |
| 005 | MySQL 8.4 defaults polish (charset, redo, I/O, packet) | P2 | S | 001 | DONE |
| 006 | Persist MySQL error + slow query logs | P2 | S | — | DONE |
| 007 | Fix MySQL credential DX (DB_* + no password print) | P2 | S | 003 | DONE |
| 008 | manage.py db backup / restore / list-backups | P1 | M | 003 | DONE |
| 011 | Escape MySQL database grant patterns for usernames | P1 | S | — | DONE |
| 009 | manage.py db shell / list / create / user-reset | P2 | M | 003, 007, 011 | DONE |
| 010 | Document recovery model; optional binlog profile | P3 | S–M | 008 | DONE |
| 012 | App isolation model (app slug primary unit) | P1 | L | — (consume 011 grant helper if present) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Dependency notes

- **001 before 004/005:** session-buffer memory must be fixed first so buffer-pool sizing math is honest.
- **003 before 008/009:** backup and db commands must not put the root password on `ps` argv.
- **007 before 009 (soft):** `db user-reset` / credentials display should follow the no-print / dual-env pattern from 007.
- **011 before 009:** DB-management commands should reuse the escaped grant-pattern helper instead of copying the old raw `username\_%` pattern.
- **008 before 010:** do not enable binlog/PITR until logical backup/restore exists.
- **002 is independent** of 001/003; can run in parallel with 001.
- **012 is independent of MySQL perf plans** but should reuse plan 011’s escaped grant pattern if already merged. If 008/009 land first, rekey their CLI from “username” to **app_name** (same string, new product noun). Prefer landing **012 before or with 009** so DB management never documents the old user/site model.

## Requested MySQL improvement mapping

These map to the user's requested items from the 2026-07-09 MySQL review:

- **3. First-class backup / restore commands:** `plans/008-mysql-backup-restore-cli.md`.
- **4. Slow query logging:** `plans/006-mysql-error-and-slow-logs.md`.
- **5. Tuning profiles / safer sizing:** `plans/001-tame-mysql-session-buffers.md`, `plans/004-env-driven-innodb-buffer-pool.md`, and `plans/005-mysql-84-defaults-polish.md`.
- **6. DB grant safety:** `plans/011-escape-mysql-grant-patterns.md`.

## Findings considered and rejected

- None rejected in this pass — all eleven MySQL findings were selected for plans.
- Host-level MySQL install / switching to MariaDB / managed cloud RDS: out of product direction for VibeOps (Docker multi-tenant LEMP ops stack).
- Adminer/phpMyAdmin Compose profile: direction-only; not planned here.
- Per-user `MAX_USER_CONNECTIONS`: direction-only; not planned here.

## Verification baseline (repo)

This repo is pure-Python + Compose config. There is **no** unit test suite.

| Purpose | Command | Expected |
|---------|---------|----------|
| Python syntax | `python3 -m py_compile manage.py` | exit 0 |
| CLI help | `./manage.py --help` | exit 0, lists commands |
| Compose parse | `docker compose config -q` | exit 0 (needs `.env` with `MYSQL_ROOT_PASSWORD`) |
| MySQL conf syntax (when up) | `docker compose exec -T mysql mysqld --help --verbose >/dev/null` | exit 0 after conf change + recreate |
